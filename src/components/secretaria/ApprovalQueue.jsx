import { Check, CheckCircle2, Phone, Trash2 } from 'lucide-react';
import Button from '../ui/Button';

export default function ApprovalQueue({ aprovacoes, onApprove, onDelete }) {
  return <div className="space-y-6"><div><h3 className="text-2xl font-black text-stone-800">Fila de Aprovações</h3><p className="text-sm text-stone-500 mt-1 font-medium">Revise os cadastros enviados pelo link público antes de incluí-los na base oficial.</p></div>{aprovacoes.length === 0 ? <div className="p-16 text-center bg-white rounded-[32px] border border-stone-200 shadow-sm"><CheckCircle2 className="mx-auto text-emerald-400 mb-4" size={48}/><h4 className="text-xl font-black text-stone-800 mb-2">Fila Vazia!</h4><p className="text-stone-500 font-medium">Não há autocadastro pendente.</p></div> : <div className="grid grid-cols-1 gap-4">{aprovacoes.map(ap => <div key={ap.id} className="bg-white p-6 rounded-[24px] border border-orange-200 shadow-sm flex flex-col md:flex-row justify-between items-start md:items-center gap-6"><div><h4 className="text-lg font-black text-stone-800">{ap.nome}</h4><p className="text-sm text-stone-500 font-medium flex items-center gap-2 mt-1"><Phone size={14}/>{ap.whatsapp || 'Sem WhatsApp'}</p><p className="text-xs text-stone-400 mt-2 font-bold">Origem: {ap.origem || 'Autocadastro'}</p></div><div className="flex gap-2 w-full md:w-auto"><Button variant="success" onClick={() => onApprove(ap)} className="flex-1 md:flex-none"><Check size={18}/> Oficializar</Button><Button variant="danger" onClick={() => onDelete('aprovacoes', ap.id)} className="flex-1 md:flex-none"><Trash2 size={18}/> Excluir</Button></div></div>)}</div>}</div>;
}
